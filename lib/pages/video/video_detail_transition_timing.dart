/// Shared timeline for the video detail Hero, route, entry skeleton and
/// skeleton-to-detail reveal.
///
/// 500 ms maps to 30, 45 and 60 refresh intervals at 60, 90 and 120 Hz.
const videoDetailTransitionDuration = Duration(milliseconds: 500);
