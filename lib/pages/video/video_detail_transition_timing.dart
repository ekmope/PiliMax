/// Shared timeline for the video detail Hero, route, entry skeleton and
/// skeleton-to-detail reveal.
///
/// 450 ms maps to 27 refresh intervals at 60 Hz and 54 at 120 Hz.
const videoDetailTransitionDuration = Duration(milliseconds: 450);
