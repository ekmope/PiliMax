/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
key.properties
**/*.keystore
**/*.jks

/build
/.kotlin
.kotlin

# Rust 核心产物由 CI 通过 `cargo ndk` 生成，不提交到仓库。
app/src/main/jniLibs/**/*.so